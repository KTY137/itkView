# itkFlow watched-folder agent

Thin client for instrument PCs (phase 2): watches a configured folder for
measurement output files and uploads them to the itkFlow ingestion inbox.
All parsing and validation happens server-side — this agent only watches,
uploads, and reports.

Not implemented yet; arrives with the ingestion service in phase 2.
