{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "itkFlow",
  "mainBinaryName": "itkflow-desktop",
  "identifier": "org.itkflow.desktop",
  "build": {
    "frontendDist": "../build/flow/splash"
  },
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "itkFlow",
        "width": 1440,
        "height": 900,
        "minWidth": 1024,
        "minHeight": 640,
        "resizable": true,
        "center": true
      }
    ]
  },
  "bundle": {
    "externalBin": ["binaries/itkflow-server"],
    "shortDescription": "ITk strip module production cockpit",
    "longDescription": "itkFlow bundles its backend and web UI into one desktop application. The ITk Production Database stays the source of truth; reads start after a user connects personal access codes, while writes remain restricted to registered DUMMY-batch test components."
  }
}
