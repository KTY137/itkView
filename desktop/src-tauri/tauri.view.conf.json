{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "itkView",
  "mainBinaryName": "itkview",
  "identifier": "org.itkflow.view",
  "build": {
    "frontendDist": "../build/view/splash"
  },
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "itkView",
        "width": 1440,
        "height": 900,
        "minWidth": 1024,
        "minHeight": 640,
        "resizable": true,
        "center": true
      }
    ]
  },
  "bundle": {
    "externalBin": ["binaries/itkview-server"],
    "shortDescription": "Read-only ITk strip module production viewer",
    "longDescription": "itkView packages the shared itkFlow backend and web UI as a read-only production viewer. It can mirror components, evidence, images and plots from the ITk Production Database after a user connects personal access codes, but it cannot ingest results, stage changes or submit writes to the PDB."
  }
}
