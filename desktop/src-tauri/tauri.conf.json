{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "itkView",
  "version": "0.2.1",
  "mainBinaryName": "itkview",
  "identifier": "org.itkflow.view",
  "build": {
    "frontendDist": "../splash"
  },
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "itkView",
        "width": 1440,
        "height": 900,
        "minWidth": 1024,
        "minHeight": 640,
        "resizable": true,
        "center": true
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": ["nsis"],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "externalBin": ["binaries/itkview-server"],
    "shortDescription": "Read-only ITk strip module production viewer",
    "longDescription": "itkView packages the shared backend and web UI as a read-only production viewer. It can mirror components, evidence, images and plots from the ITk Production Database after a user connects personal access codes, but it cannot ingest results, stage changes or submit writes to the PDB."
  }
}
